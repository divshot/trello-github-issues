<h1 class="page-header">Hook Setup</h1>
<div class="row">
  <form method='post' role="form" class="col-md-6">
    <h2>List Options</h1>
    <p>Select which list to move the card to when:</p>
    <!--<div class="form-group">
      <label for="opened_list_id">Issue is Opened:</label>
      <select name="hook[opened_list_id]" id="opened_list_id" class="form-control">
        <option value=''>Do Nothing</option>
        <%= optionize(@lists, @hook.opened_list_id) %>
      </select>
    </div>-->
  
    <div class="form-group">    
      <label for="closed_list_id">Issue is Closed:</label>
      <select name="hook[closed_list_id]" id="closed_list_id" class="form-control">
        <option value=''>Do Nothing</option>
        <%= optionize(@lists, @hook.closed_list_id) %>
      </select>
    </div>
  
    <div class="form-group">
      <label for="reopened_list_id">Issue is Reopened:</label>
      <select name="hook[reopened_list_id]" id="reopened_list_id" class="form-control">
        <option value=''>Do Nothing</option>
        <%= optionize(@lists, @hook.reopened_list_id) %>
      </select>
    </div>
    
    <button type="submit" class="btn btn-primary btn-block">Save</button>
  </form>
  <div class="col-md-6">
  <h2>Add to GitHub</h1>
  
  <p>Remember to enable the <b>Issues</b> event when creating the Webhook.</p>
  
    <input type='text' value='<%= request.url %>/payload' readonly class="form-control" style="font-size: 11px;">
  </div>
</div>